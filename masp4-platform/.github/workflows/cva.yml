name: Climate Vulnerability Dashboard Refresh

# Keeps the Climate Vulnerability Assessment dashboard in sync with the live Kobo form:
#   public/Climate_Vulnerability_Dashboard.html
# Flow: fetch Kobo (JSON) -> defensive clean/transform -> upsert Supabase (cva_* tables)
#       -> rebuild dashboard -> commit (triggers Vercel deploy).
#
# Triggers: nightly 03:40 UTC (offset from seedlings 03:00-ish and hc_survival 03:20),
#           on pipeline source change, or manual.
#
# Required secrets (Settings -> Secrets and variables -> Actions):
#   KOBO_TOKEN                    KoBoToolbox API token
#   NEXT_PUBLIC_SUPABASE_URL      Supabase project URL (MASPIV_Platform)
#   SUPABASE_SERVICE_ROLE_KEY     Supabase service role key (server-side only)
on:
  schedule:
    - cron: "40 3 * * *"      # 03:40 UTC daily (06:40 EAT)
  push:
    paths:
      - "pipeline/cva/**"
      - ".github/workflows/cva.yml"
  workflow_dispatch:

concurrency:
  group: cva-refresh
  cancel-in-progress: false

jobs:
  refresh:
    name: Fetch Kobo → clean → Supabase → rebuild dashboard
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"
          cache-dependency-path: pipeline/cva/requirements.txt

      - name: Install dependencies
        run: pip install -r pipeline/cva/requirements.txt

      # 1. Pull all submissions + the form definition (JSON, gitignored)
      - name: Fetch Kobo submissions
        env:
          KOBO_TOKEN: ${{ secrets.KOBO_TOKEN }}
        run: python pipeline/cva/fetch_cva_json.py

      # 2. Defensive clean/normalize -> clean_*.json
      - name: Transform (defensive clean)
        run: python pipeline/cva/transform.py

      # 3. Upsert raw + normalized rows into Supabase (cva_* tables), idempotent
      - name: Load Supabase
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: python pipeline/cva/load_supabase.py

      # 4. Rebuild the self-contained dashboard into public/
      - name: Rebuild dashboard
        run: python pipeline/cva/build_dashboard.py

      # 5. Commit regenerated dashboard (only when changed). Push triggers Vercel deploy.
      - name: Commit updated dashboard
        run: |
          if git diff --quiet -- public/Climate_Vulnerability_Dashboard.html; then
            echo "No dashboard changes — nothing to commit."
            exit 0
          fi
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add public/Climate_Vulnerability_Dashboard.html
          git commit -m "chore: climate-vulnerability dashboard refresh $(date -u '+%Y-%m-%d %H:%M UTC')"
          n=0
          until git push; do
            n=$((n+1))
            if [ "$n" -ge 5 ]; then echo "::error::push failed after $n attempts"; exit 1; fi
            echo "push rejected (main moved); rebasing — attempt $n"
            git fetch origin "${GITHUB_REF_NAME}"
            git rebase "origin/${GITHUB_REF_NAME}" \
              || { echo "::error::rebase conflict on generated dashboard"; git rebase --abort; exit 1; }
          done
