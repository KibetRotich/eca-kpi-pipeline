# Service account credentials — NEVER commit these
*.json
!package.json

# Large data files — keep out of git root
*.pdf
*.csv
# But allow CSVs placed in data/ to trigger the pipeline
!data/*.csv

# Build artifacts
node_modules/
__pycache__/
*.pyc
.env
